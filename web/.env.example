# Grow Core base URL. Leave unset in production — the web app is served by
# Grow Core itself (same origin). Set this only for local dev when running the
# SvelteKit dev server separately from Grow Core.
# VITE_GROWCORE_URL=http://localhost:8080
