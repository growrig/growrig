// Grow App Web is a client-rendered SPA served by Grow Core. Disable SSR and
// prerendering so every route is handled in the browser against the live API.
export const ssr = false;
export const prerender = false;
